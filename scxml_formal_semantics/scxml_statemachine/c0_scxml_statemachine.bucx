/*
 * This context declares the dequeued triggers
 * @author cfsnook
 */
context c0_scxml_statemachine 
extends scxml_c0_abstract c1_statemachine
constants
	triggeredTransitions // trigger - transition relationship
	finalisedTransitions   // the transitions which can no longer be refined
axioms
	@tr_tr: 	triggeredTransitions ∈ transitions ⇸ TRIGGERS 
	@finalised_tr: 	finalisedTransitions ⊆ transitions
end